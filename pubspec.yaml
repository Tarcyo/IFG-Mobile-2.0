name: ifg_mobile_estudante
description: "Aplicativo dos estudantes do Instituto Federal De Goiás."

publish_to: 'none' 

environment:
  sdk:  ^	3.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter 

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1
  dio: ^5.8.0+1
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.4
  get_it: ^8.0.3
  flutter_dotenv: ^5.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 
    - .env
    - assets/data/

  fonts:
    - family: Quicksand
      fonts:
        - asset: assets/fonts/Quicksand/Quicksand.ttf 
